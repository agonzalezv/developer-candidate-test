## Simple react + graphql frontend

### Initializing

- `npm i`
- `npm run start`

### Requirements

Make sure the [graphql backend](../backend/README.md) is running. 

- `cd ../backend`
- `npm run start`
- open http://localhost:3000 in your browser.
